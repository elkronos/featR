% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_pca.R
\name{fs_pca}
\alias{fs_pca}
\title{Principal component analysis with tidy results and optional plotting}
\usage{
fs_pca(
  data,
  num_pc = NULL,
  scale_data = TRUE,
  center_data = TRUE,
  label_col = NULL,
  plot = !is.null(label_col),
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame or data.table with at least two rows and at least
one numeric column.}

\item{num_pc}{Number of principal components to retain, or \code{NULL} (the
default) to retain \code{min(2, max_possible)} where \code{max_possible} is
\code{min(nrow - 1, ncol)} of the usable numeric data. Explicit values larger
than \code{max_possible} raise an error.}

\item{scale_data}{Logical; scale numeric columns to unit variance.
Default \code{TRUE}.}

\item{center_data}{Logical; center numeric columns. Default \code{TRUE}.}

\item{label_col}{Optional name of a column used to label and color points;
the column is excluded from the PCA features.}

\item{plot}{Logical; if \code{TRUE} (the default whenever \code{label_col} is
supplied) and at least two components are available, a PC1 vs PC2
scatterplot is printed and returned as \verb{$plot}.}

\item{verbose}{Logical; emit progress messages. Default \code{TRUE}.}
}
\value{
A list with components:
\itemize{
\item \code{pc_loadings}: matrix of variable loadings (features x PCs).
\item \code{pc_scores}: matrix of observation scores (rows kept x PCs).
\item \code{var_explained}: proportion of total variance per retained PC
(see Details for the large-data engine).
\item \code{pca_df}: data.table of scores plus label columns.
\item \code{meta}: list with \code{numeric_cols}, \code{rows_kept}, \code{n_rows_used},
\code{n_cols_used}.
\item \code{plot}: the ggplot object (only when a plot was produced).
}
}
\description{
Runs a PCA on the numeric columns of \code{data}. Character and factor columns
are excluded from the feature set and kept as label candidates; an
explicitly supplied \code{label_col} (numeric or not) is likewise excluded from
the features and only used for labeling. Rows with missing values in the
numeric columns and zero-variance columns are dropped before the
decomposition.
}
\details{
For small data (fewer than 1e7 cells) the decomposition uses
\code{stats::prcomp()}. For larger data it uses \code{bigstatsr::big_SVD()} on a
temporary file-backed matrix (the backing file is deleted on exit) when the
suggested package 'bigstatsr' is installed; otherwise it falls back to
\code{prcomp()} with a message.

\code{var_explained} always reports the proportion of \emph{total} variance
explained by each retained component. With the 'bigstatsr' engine only the
retained components are computed, so the entries do not sum to 1 (they sum
to the fraction of variance captured by those components). Plotting
requires the suggested package 'ggplot2'.
}
\examples{
res <- fs_pca(mtcars, num_pc = 2, label_col = "cyl", plot = FALSE)
res$var_explained
head(res$pca_df)

\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  res <- fs_pca(iris, label_col = "Species")
}
}
}
